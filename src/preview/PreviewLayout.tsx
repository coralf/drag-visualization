import * as React from 'react';


export interface PreviewLayoutProps {
  
}

const PreviewLayout: React.SFC<PreviewLayoutProps> = (props) => {
  return (
    <div>
      {'PreviewLayout'}
    </div>
  );
}

export default PreviewLayout;